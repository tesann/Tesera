[project]
name = "tes-core"
version = "0.1.0"
description = "Tesera core SDK — hashing, signing, commits, store, chain"
requires-python = ">=3.11"
dependencies = ["python-magic"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tes_core"]
