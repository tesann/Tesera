# Tesera
Tesera proposes an open infrastructure layer that records the origin and evolution of digital media using cryptographic records. The system functions similarly to Git for software, but instead tracks the lifecycle of images and videos.
